#!/usr/bin/env bash
set -e
set -u
set -o pipefail

action="${TOOLBOX_ACTION:-}"

if [[ "$action" == "describe" ]]; then
  echo "name: week"
  echo "description: Prints the current week number."
elif [[ "$action" == "execute" ]] || [[ -z "$action" ]]; then
  echo -n "$(date +%V)"
fi