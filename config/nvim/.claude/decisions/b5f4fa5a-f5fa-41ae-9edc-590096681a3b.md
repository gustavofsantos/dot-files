1. Used visual-mode `<CR>` as the trigger key, mapped as `:AgentCommentAdd<CR>` (a range-aware user command) rather than a raw Lua callback reading `'<`/`'>` marks manually — verify `<CR>` in Visual mode doesn't collide with a Visual-mode Enter binding you already rely on elsewhere.
2. Comment storage is purely in-memory (a module-level Lua table, never written to disk until flush) — verify that's acceptable given comments are lost on `:qa`/Neovim crash before running `:AgentCommentsFlush`.
3. Used a floating scratch buffer (filetype `markdown`, `startinsert` on open) for the comment input instead of `vim.ui.input`, to support multi-line comments and custom `<C-s>`/`<Esc>` buffer-local keymaps — verify the popup's size/position (relative to cursor, ~50% editor width, 6 lines tall) is usable in your actual splits/tmux layout.
4. Sign column indicator uses the 💬 emoji via `nvim_buf_set_extmark` (`sign_text`) rather than a plain ASCII glyph — verify it renders correctly (not as a double-width glitch or tofu box) in your terminal/font (Ghostty/wezterm) and signcolumn width.
5. On `:AgentCommentsFlush`, the plugin both (a) writes the rendered prompt to a `vim.fn.tempname()` file opened in a new `vsplit`, and (b) auto-copies the same content into the `+` and unnamed `"` registers, then clears all pending comments/signs — verify the auto-clipboard-copy is wanted (it clobbers whatever was already in those registers) and that clearing-on-flush (vs. keeping comments until manually cleared) matches how you intend to use it.
6. Chose command names `AgentCommentAdd` / `AgentCommentsFlush` / `AgentCommentsClear` and leader keymaps `<leader>cf` / `<leader>cx` under a new which-key group `<leader>c` ("Agent Comments") — verify `<leader>c` wasn't silently reserved for something else you plan to add later, and that `cf`/`cx` read as mnemonic enough (flush / discard-"x").
7. Placed the implementation as a new self-contained module `lua/personal-plugins/agent-comments.lua` plus `after/plugin/agent-comments.config.lua` (just a `require`), following the existing `super-return.lua`/`project-bookmarks.lua` pattern of defining commands inside the module itself rather than exposing a `setup()` config table — verify no per-machine configuration (e.g. sign glyph, popup size, temp-file location) is something you'll actually want to tweak later, since none is currently exposed.
